# ===========================================
# ClickUp MCP Server Configuration
# ===========================================
# Copy this file to .env and fill in your values
# See docs/CONFIGURATION.md for detailed documentation

# ----- Required -----
# Encryption key: Generate with `openssl rand -hex 32`
CLICKUP_MASTER_KEY=

# PostgreSQL connection string
DATABASE_URL=postgres://mcp:mcp@localhost:5432/mcp

# ----- Server -----
PORT=3000
# BASE_URL=https://your-domain.com  # Set if behind a proxy
TRANSPORT=http

# ----- Authentication -----
# API key mode: disabled | global | user_bound
CLICKUP_API_KEY_MODE=user_bound

# Allowed OAuth redirect URIs (comma-separated)
CLICKUP_REDIRECT_URI_ALLOWLIST=http://localhost:3000/callback
CLICKUP_REDIRECT_URI_ALLOWLIST_MODE=prefix

# ----- Session -----
CLICKUP_CODE_TTL_SECONDS=90
CLICKUP_TOKEN_TTL_SECONDS=3600

# ----- ClickUp Defaults (Optional) -----
# CLICKUP_API_TOKEN=pk_...
# CLICKUP_TEAM_ID=
WRITE_MODE=write
CHAR_LIMIT=16000
